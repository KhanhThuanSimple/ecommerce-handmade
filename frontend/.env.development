# API: dùng đường dẫn tương đối để setupProxy.js chuyển tiếp sang :8080
REACT_APP_API_URL=/api

# Backend gốc (cho setupProxy)
REACT_APP_BACKEND_URL=http://localhost:8080

# URL FE (khớp app.frontend-url trong application.properties)
REACT_APP_FRONTEND_URL=http://localhost:3000
